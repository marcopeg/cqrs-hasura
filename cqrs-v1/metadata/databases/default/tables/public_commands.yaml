table:
  schema: public
  name: commands
event_triggers:
  - name: foobar
    definition:
      enable_manual: false
      insert:
        columns: '*'
    retry_conf:
      num_retries: 0
      interval_sec: 10
      timeout_sec: 60
    webhook: http://google.com
